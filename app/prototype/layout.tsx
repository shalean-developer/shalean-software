import { PrototypeRealmLayout } from "@/components/prototype-realm/prototype-realm-layout";

export default function PrototypeLayout({ children }: { children: React.ReactNode }) {
  return <PrototypeRealmLayout>{children}</PrototypeRealmLayout>;
}
