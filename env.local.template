# Copy to `.env.local` and fill in values (never commit `.env.local`).
# See `.env.example` for descriptions.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

PAYSTACK_SECRET_KEY=
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=

NEXT_PUBLIC_SITE_URL=http://localhost:3000

RESEND_API_KEY=
NOTIFICATIONS_FROM_EMAIL=
NOTIFICATIONS_FROM_NAME=
NOTIFICATIONS_ADMIN_EMAILS=

NOTIFICATIONS_CRON_SECRET=
NOTIFICATION_OUTBOX_LEASE_SECONDS=180

RECONCILIATION_OVERRIDE_ENABLED=false
RECONCILIATION_OVERRIDE_SECRET=

TEMP_ROLE_DIAG_ENABLED=

# Legacy (Vercel Cron only — optional if using Supabase scheduler)
# CRON_SECRET=
