# Supabase CLI configuration — link with `supabase link --project-ref <ref>`
# Docs: https://supabase.com/docs/guides/cli/config
#
# Notification outbox draining is scheduled against your Next.js app via
# pg_cron + pg_net + Vault (see supabase/scripts/schedule-notification-outbox-worker.sql).

[api]
enabled = true
